server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # React Router SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }

  # /api/* 는 CloudFront가 ALB → ECS server로 라우팅 — nginx는 처리 안 함
}
